---
- hosts: loadBalancer
  become: true
  tasks:
    - name: install web components
      apt: name=['nginx', 'python-pip', 'python-virtualenv'] state=present update_cache=yes

    - name: ensure nginx started
      service: name nginx state=started enabled=yes
